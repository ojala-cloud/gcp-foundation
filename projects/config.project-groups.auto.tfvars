#
# Common settings for project groups
#

#
# For project group settings, the key is the group name (referenced from project configuration)
# and value is the specific value, e.g. labels (map(string)) or list of APIs to enable.
#

#
# Project group labels
#

default_project_group_labels = {
  "heroku" = {
    "application" = "heroku"
  }
}
